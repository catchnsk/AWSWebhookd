apiVersion: v1
kind: ServiceAccount
metadata:
  name: delivery-listener-sa
  namespace: delivery
  labels:
    app: delivery-listener
  annotations:
    # IRSA: IAM role ARN with permissions to DynamoDB, MSK, CloudWatch
    # Replace with your actual IAM role ARN
    eks.amazonaws.com/role-arn: "arn:aws:iam::123456789012:role/delivery-listener-role"
